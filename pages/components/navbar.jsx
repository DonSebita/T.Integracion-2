import React from 'react';

const Navbar = (props) => {
    return (
        <div className={props.className + " h-min bg-slate-100"}>
            <h1 className="text-black">NAVBAR</h1>
        </div>
    );
}

export default Navbar;
