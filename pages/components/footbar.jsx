import React from 'react';

const Footbar = (props) => {
    return (
        <div className={props.className + "h-min bg-slate-700"}>
            <h1>FOOTBAR</h1>
        </div>
    );
}

export default Footbar;

