import React from 'react';

const Footbar = (props) => {
    return (
        <footer className={props.className + "h-min bg-slate-700"}>
            <h1>FOOTBAR</h1>
        </footer>
    );
}

export default Footbar;

